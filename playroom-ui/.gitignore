node_modules
dist
src/generated
*.tgz

# The repo root .gitignore has a blanket `models/` rule (for python training
# checkpoints elsewhere in this repo) that would otherwise also catch our
# actual 3D model assets here.
!/public/assets/models/
!/public/assets/models/**
